"use client";

import { Button } from "@/components/ui/button";
import { Plus } from "lucide-react";

export const DirectMessagesSidebar = () => {
  return (
    <div className="flex-1">
      {/* This component is now integrated into the FriendsSidebar */}
      {/* The direct messages section is handled in the main FriendsSidebar component */}
    </div>
  );
}; 